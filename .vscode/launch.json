{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Stellar Resource Usage",
      "type": "node-terminal",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "command": "npm run start"
    }
  ]
}
